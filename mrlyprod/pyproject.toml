[build-system]

requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]

name = "mrlyprod"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "Pillow",
]

[tool.setuptools.packages.find]

include = ["mrlypy", "mrlypy.*", "utils"]
